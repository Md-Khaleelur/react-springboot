apiVersion: v1
kind: Service
metadata:
    name: {{.Values.bdiscovery.service.name}}
    labels:
        name: {{.Values.bdiscovery.service.labels.name}}
        app: {{.Values.bdiscovery.service.labels.app}}
spec:
    ports:
        - port: {{.Values.bdiscovery.service.port}}
          targetPort: {{.Values.bdiscovery.service.targetPort}}
    selector:
        name: {{.Values.bdiscovery.service.selector.name}}
        app: {{.Values.bdiscovery.service.selector.app}}
